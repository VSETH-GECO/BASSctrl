/**
 * This file is fine.
 * Just ignore the tslint errors.
 */

export enum Resource {
  APP = 'app',
  USER = 'user',
  TRACK = 'track',
  QUEUE = 'queue',
  PLAYER = 'player',
  FAVORITES = 'favorites'
}

export enum Action {
  GET = 'get',
  SET = 'set',
  ADD = 'add',
  DELETE = 'delete',
  LOGIN = 'login',
  LOGOUT = 'logout',
  INFORM = 'inform',
  VOTE = 'vote',
  SUCCESS = 'success',
  ERROR = 'error',
  DATA = 'data',
  UPDATE = 'update'
}
