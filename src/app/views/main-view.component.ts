import {Component} from '@angular/core';

@Component({
  selector: 'app-main-view',
  templateUrl: './main-view.component.html',
  styleUrls: ['../app.component.css']
})
export class MainViewComponent {
  // Might be needed by the compiler from time to time. I have no idea why. Maybe ask google?

  // requestUri;
  // submitPending;
  // reload;
  // setUsername;
  // username;
  // setPlaylist;
}
